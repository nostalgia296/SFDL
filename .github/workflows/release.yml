name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26'

    - name: Build all platforms
      run: |
        mkdir -p dist
        GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o dist/sfdl-linux-amd64 ./cmd/sfdl
        GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o dist/sfdl-linux-arm64 ./cmd/sfdl
        GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o dist/sfdl-windows-amd64.exe ./cmd/sfdl
        GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o dist/sfdl-darwin-amd64 ./cmd/sfdl
        GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o dist/sfdl-darwin-arm64 ./cmd/sfdl
        GOOS=android GOARCH=arm64 go build -ldflags="-s -w" -o dist/sfdl-android-arm64 ./cmd/sfdl

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*
        generate_release_notes: true
